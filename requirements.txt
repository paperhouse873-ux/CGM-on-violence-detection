# Python dependencies for the CGM code.
#
# Note on PyTorch: torch / torchvision are intentionally NOT pinned here.
# On a GPU box the CUDA build is usually already installed, and reinstalling
# torch over it tends to break CUDA. If you don't have torch yet, install the
# CUDA build first from https://pytorch.org, then run:
#   pip install -r requirements.txt

# video backbones (X3D-S, SlowFast, MViT) via pytorchvideo.models.hub
pytorchvideo==0.1.5

# video/image processing — headless build to avoid libGL issues on servers
opencv-python-headless

# CGM training, metrics, preprocessing
scikit-learn
numpy<2.3
tqdm

# figures (phase7 / phase9 / phase10)
matplotlib

# Optional: only needed to extract context streams from raw video (YOLO crowd
# counting in phase3). Not required for fine-tuning, which reuses cached
# context features. Uncomment if you re-run phase3:
# ultralytics
